# ===========================================
# KUCHABICHO BACKEND - VARIABLES DE ENTORNO
# ===========================================
# Copia este archivo como .env y completa los valores
# NUNCA subas el archivo .env a git

# ===== DATABASE (MySQL) =====
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_secure_password_here
DB_NAME=kuchabicho
DB_PORT=3306

# ===== SESSION =====
# Genera una clave segura de al menos 32 caracteres
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS

# ===== SERVER =====
NODE_ENV=development
PORT=3001

# ===== CORS =====
# URL del frontend (sin trailing slash)
FRONTEND_URL=http://localhost:5173

# ===== CSRF =====
# Secreto para tokens CSRF (diferente al de sesión)
CSRF_SECRET=CHANGE_THIS_TO_ANOTHER_SECURE_RANDOM_STRING
